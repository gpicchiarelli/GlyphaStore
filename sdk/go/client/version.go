package client

// Version is the official Go client semantic version. It must match the repository
// root VERSION file and other official SDK 0.x releases (see scripts/check-sdk-versions.sh).
const Version = "0.1.0"
