# Actionlint config: allow documented self-hosted runner labels.
# paired-linux-performance.yml is workflow_dispatch-only and requires
# glyphastore-linux-perf; hosted ubuntu-latest cannot close that gate.
self-hosted-runner:
  labels:
    - glyphastore-linux-perf
